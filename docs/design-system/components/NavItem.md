Enlace de la barra lateral con icono Lucide de 19px y texto; el activo se rellena con el color de tema.

- Agrupa con `ej-nav-label` (HOY, APRENDER, PRACTICAR, CONSULTAR) en `text-faint`.
- Activo: `ej-nav-item--active` + `aria-current="page"`. Solo uno.
- Inactivo: `text-secondary`; hover `surface-raised`.
- Trazo 1.8 en inactivos; el activo puede usar la versión rellena del mismo icono.
