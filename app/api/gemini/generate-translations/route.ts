import { NextRequest, NextResponse } from 'next/server'
import { z } from 'zod'
import { GENERATE_TRANSLATIONS_SYSTEM_PROMPT, buildGenerateTranslationsPrompt } from '@/lib/ai-prompts'
import { requireSameOrigin, requireUser, checkLayeredRateLimit, validateBody } from '@/lib/api/guards'
import { parseGeminiJson, respondWithGeminiJson } from '@/lib/gemini/json-route'
const RequestSchema = z.object({ topic: z.string().trim().min(2).max(120), level: z.string().min(2).max(4), count: z.number().int().min(1).max(5).default(2) }).strict()
const ResponseSchema = z.object({ exercises: z.array(z.object({ sourceEs: z.string().min(2).max(300), referenceEn: z.string().min(2).max(300), acceptedAnswers: z.array(z.string().min(2).max(300)).max(3).optional() }).strict()).min(1).max(5) }).strict()
export async function POST(request: NextRequest): Promise<NextResponse> { const originError = requireSameOrigin(request); if (originError) return originError; const { user, error: authError } = await requireUser(request); if (authError) return authError as NextResponse; const { limited, error: rateLimitError } = await checkLayeredRateLimit({ request, user, endpoint: '/api/gemini/generate-translations', maxPermanent: 10, maxAnonymous: 3 }); if (limited) return rateLimitError as NextResponse; const { data, error } = await validateBody(request, RequestSchema); if (error) return error as NextResponse; return respondWithGeminiJson({ endpoint: '/api/gemini/generate-translations', userId: user.id, params: { contents: buildGenerateTranslationsPrompt(data), config: { systemInstruction: GENERATE_TRANSLATIONS_SYSTEM_PROMPT, responseMimeType: 'application/json', temperature: 0.4, maxOutputTokens: 1024 } }, parse: (raw) => parseGeminiJson(raw, (json) => ResponseSchema.parse(json)), failureMessage: 'Failed to generate translations' }) }
