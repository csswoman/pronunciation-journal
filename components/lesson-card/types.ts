export type LessonCoverProps = {
  imageUrl: string | null;
  title: string;
};

export type OwnershipBadgeProps = {
  isSystem: boolean;
};

export type LessonBadgesProps = {
  category: string | null;
  level: string | null;
  isSystem: boolean;
};

export type LessonTitleProps = {
  title: string;
};
