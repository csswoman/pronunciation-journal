import { CtaButtons, ResumeButton } from "./PageHeaderButtons";
import type { PageHeaderDerived } from "./types";

export function DefaultCompactHeader({ badge, title, subtitle, description, primaryCta, secondaryCta, illustration, variant, hasProgress, safeProgress, lessonTitle, phonemeLabel, onContinue, className = "" }: PageHeaderDerived) {
  const isCompact = variant === "compact";
  return <div className={["relative overflow-hidden", "grid grid-cols-1 lg:grid-cols-2 items-center", isCompact ? "gap-8 p-6 lg:p-8" : "gap-10 p-8 lg:p-12", className || "rounded-[15px_15px_0_0]"].join(" ")} style={{ background: "var(--surface-raised)", border: "1px solid var(--border-subtle)", boxShadow: "var(--shadow-md)" }}><div className="relative z-10 max-w-xl">{badge && <div className="inline-flex items-center mb-5 px-3 py-1" style={{ background: "var(--overlay-subtle)", border: "1px solid var(--border-subtle)", borderRadius: "var(--radius-full)" }}><span style={{ font: "var(--font-tiny)", color: "var(--text-tertiary)", textTransform: "uppercase", letterSpacing: "0.08em" }}>{badge}</span></div>}<div><span className="block" style={{ font: "var(--font-h3)", color: "var(--text-primary)" }}>{title}</span>{subtitle && <span className="block" style={{ font: "var(--font-h3)", color: "var(--primary)" }}>{subtitle}</span>}</div>{description && <p className="mt-1" style={{ font: "var(--font-body-sm)", color: "var(--text-secondary)" }}>{description}</p>}{hasProgress ? <div className="mt-6 space-y-3"><div><div className="flex items-center gap-2 mb-2">{phonemeLabel && <span style={{ background: "var(--primary-soft)", color: "var(--primary)", borderRadius: "var(--radius-full)", font: "var(--font-tiny)", padding: "2px 8px" }}>{phonemeLabel}</span>}<span className="truncate" style={{ font: "var(--font-body-sm)", fontWeight: 500, color: "var(--text-primary)" }}>{lessonTitle}</span><span className="ml-auto shrink-0 tabular-nums" style={{ font: "var(--font-tiny)", color: "var(--text-tertiary)" }}>{safeProgress}%</span></div><div style={{ height: "4px", borderRadius: "var(--radius-full)", background: "var(--overlay-subtle)", overflow: "hidden" }}><div className="h-full transition-all duration-500" style={{ width: `${safeProgress}%`, borderRadius: "var(--radius-full)", background: "var(--primary)" }} /></div></div>{onContinue && <ResumeButton onClick={onContinue} />}</div> : (primaryCta || secondaryCta) && <div className="mt-8 flex gap-4 flex-wrap"><CtaButtons primaryCta={primaryCta} secondaryCta={secondaryCta} rounded="md" /></div>}</div>{illustration && <div className="relative z-10 flex items-center justify-center" style={{ minHeight: "260px" }}><div className="relative w-full [&_img]:w-full [&_img]:h-auto [&_svg]:w-full [&_svg]:h-auto drop-shadow-xl" style={{ maxWidth: "420px" }}>{illustration}</div></div>}</div>;
}
