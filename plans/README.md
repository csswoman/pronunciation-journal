# Implementation Plans

Generated by the improve skill on 2026-06-11.
Last reconciled: **2026-08-10**. Plans 046–057 were written against HEAD
`38c3abe5` and are now implemented per `TODO.md`; plans 059–065 were produced
from the learning-loop audit at HEAD `c779781b`; plans 066–070 extend the oral
product roadmap at HEAD `99c871cb`; plan 073 consolidates the app-wide learning
loop at HEAD `ce654374`; plans 074–075 capture the verified evidence-sanitizing
and exercise-consolidation work around that integration. Statuses through 045
preserve the earlier focused-test and linked Supabase verification recorded in
their rows.

> **Fuente de verdad para trabajo nuevo:** `TODO.md`, `plans/032-post-production-improvement-roadmap.md`
> y los planes atómicos 046–057.
> Roadmap 031 (producción) está cerrado. Este archivo recoge el historial de planes 001-030
> y el estado de planes atómicos referenciados por el roadmap 032.
>
> **Integración del aprendizaje:**
> `docs/architecture/integrated-learning-loop.md` define el contrato de producto
> y los planes se ejecutan en este orden: 074 sanea evidencia, 073 conecta las
> superficies y 075 consolida capacidades de ejercicio.

Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, and update
your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|---|---|---|---|---|---|
| 001 | Make CI actually gate — remove `continue-on-error` and fix everything that turns red | P1 | S | — | REJECTED (superseded by current CI drift; see 019) |
| 002 | Fix heatmap UTC/Lima timezone mismatch in `getDailyCompletionStats` | P2 | S | 001 | DONE (2026-06-11, commit `39aff2b` on `executor/002-heatmap-tz`, reviewer-verified; awaiting user merge) |
| 003 | Rotate all leaked credentials and purge .env.local from git history | P1 | S | — | DONE (2026-06-12: drift check confirmed .env.local was never tracked and has no git history — git steps N/A. Key rotation skipped by owner; project is personal with no external collaborators) |
| 004 | Fix admin access gate — wrong role check in AdminLayout and missing server-side auth on seed mutations | P1 | M | — | DONE (2026-07-03: `requireAdmin`, `/api/admin/seed`, inserts vía API) |
| 005 | Remove unused `ai` and `@ai-sdk/google` dependencies | P3 | S | — | DONE (implemented in `a9dcad12`; dependency/import checks reconfirmed 2026-07-17) |
| 006 | Route fire-and-forget SRS updates through the existing outbox | P2 | M | — | DONE (2026-07-03: `enqueueWordBankSRSUpdate` en `lib/practice/queries.ts`) |
| 007 | Fix STT transcription cache RLS so L2 cache reads and writes succeed | P1 | S | — | DONE (migraciones `20260611120000` + `20260621140000`; test en `lib/__tests__/stt-cache-rls-migration.test.ts`) |
| 008 | Centralize all Gemini prompt strings into `lib/ai-prompts.ts` | P1 | S | — | DONE (2026-07-03: user prompts en `build*Prompt` en `lib/ai-prompts.ts`) |
| 009 | Add pure-logic unit tests for the sync-manager outbox | P2 | M | 001 | DONE (implemented in `5a2f912f`; focused suite verified 2026-07-17) |
| 010 | Lazy-load canvas-confetti to remove it from the main bundle | P3 | S | — | DONE (implemented independently; dynamic import confirmed 2026-06-21) |
| 011 | Move COURSE_PATH_CURRICULUM out of the client bundle | P2 | M | — | REJECTED (import moved, but the full object is still serialized; superseded by 027) |
| 012 | Replace `select('*')` with explicit column lists in critical queries | P3 | S | — | REJECTED (stale inventory; superseded by 029) |
| 013 | Add unit tests to the three highest-value API routes | P3 | M | 001 | DONE (implemented in `73799fa3`; health/words/lexicon suites verified 2026-07-17) |
| 014 | Route all component Supabase calls through lib/*/queries.ts | P2 | L | 001 | REJECTED (2026-07-03: solo `AuthProvider` importa client — exento por reglas) |
| 015 | Split PracticeSession and GrammarStudyDeck below the 250-line limit | P3 | M | — | REJECTED (2026-07-03: `PracticeSession` ≤120 líneas) |
| 016 | Unified progress hub — centralize practice telemetry, radar, sounds mastery, review center | P1 | L | — | DONE (plan interno COMPLETE; Fases 0-4) |
| 017 | Exercise eligibility contract — unified lemma/context rules, GenerationResult, CI generatability gate | P1 | M | — | DONE |
| 018 | Unify pedagogical exercise feedback and beginner retry support | P1 | L | — | DONE |
| 019 | Make the current CI gates fail on broken lint, tests, audit, and token checks | P1 | S | — | DONE (2026-06-21, CI gates blocking; audit overrides added for patched transitives) |
| 020 | Finish the unified progress hub so every practice surface records the same session contract | P1 | L | 019 | DONE (2026-06-21) |
| 021 | Add an error taxonomy so beginner feedback and progress analytics explain why answers fail | P1 | M | 019 | DONE (2026-06-21) |
| 022 | Extract shared Gemini route infrastructure for fallback, validation, limits, and errors | P2 | M | 019 | DONE (extended 2026-07-03: lib/gemini/client.ts adds callWithFallback with timeout; all 7 Gemini routes migrated — T22+T23 in roadmap 031) |
| 023 | Refresh README and onboarding docs to match the current Next 16, pnpm, Supabase, Dexie app | P2 | S | — | DONE (2026-06-21) |
| 024 | Defer global client features until the user opens them | P1 | M | — | DONE (2026-06-21, implementation commit `26c3d55`) |
| 025 | Load each Words area only when its tab is active | P1 | L | 024 | DONE (2026-06-21, local working tree) |
| 026 | Cache parsed lexicon content within the server process | P2 | S | — | DONE (2026-06-21, local working tree) |
| 027 | Stop serializing the complete curriculum through a Client Component | P2 | M | — | DONE (2026-06-21, local working tree) |
| 028 | Fetch only the phoneme data required for a session | P2 | M | — | DONE (2026-07-03: `getSessionDataset(s)` + tests; sin `getAllWords` en producción) |
| 032 | Post-production improvement roadmap — security, perf, pedagogy, tests, scale | P1 | L | 031 | IN PROGRESS (T50 manual Reader QA pending; phases 1, 2 and 4 implemented; phase 5 explicitly deferred) |
| 029 | Remove remaining over-broad Supabase projections | P3 | S | — | DONE (2026-06-21, local working tree) |
| 030 | Close curriculum gaps by level, then derive placement and checkpoint tests from the curriculum | P1 | L | — | DONE (focused tests and typecheck pass; full suite has 2 unrelated transcribe mock failures) |
| 033 | Unify enrichment job lifecycle updates | P1 | S | — | DONE (2026-07-05: cron success path clears locks; regression test exists) |
| 034 | Fail word creation when deck linking fails | P1 | S | — | DONE (2026-07-05: deck lookup/link failures surface public errors; route tests exist) |
| 035 | Confirm word image database updates | P2 | S | — | DONE (2026-07-05: upload/delete metadata updates checked; route tests exist) |
| 036 | Harden sentence generation configuration and cache writes | P2 | S | — | DONE (2026-07-05: server config guarded; cache-failure degradation tested) |
| 037 | Deduplicate server auth and Supabase clients per request | P1 | M | — | DONE (2026-07-05, implementation commit `f47f5a13`) |
| 038 | Slim the authenticated shell client bundle | P1 | M | 037 | DONE (2026-07-05, implementation commit `f47f5a13`) |
| 039 | Split Words interaction chunks beyond tab runtimes | P1 | M | — | DONE (2026-07-05, implementation commit `8f797d8`) |
| 040 | Convert presentational Client Components to Server Components | P2 | L | 038 | DONE (2026-07-05: serverized safe home/layout presentation components; broader words split deferred by client tab boundary) |
| 041 | Stabilize hot client re-renders in practice and words flows | P2 | M | — | DONE (2026-07-05, implementation commit `c9e08b9`) |
| 042 | Restore the normal production build gate | P1 | S | — | DONE (2026-07-05, implementation commit `44f6acc`) |
| 043 | Weekly retention loop | P1 | M | — | TODO (conditional; activate only when retention is a measured priority) |
| 044 | Retire inactive legacy audio retention without losing the remaining recording | P1 | M | — | DONE (commit `48c5fd52`; production schema/function/cron and focused checks verified 2026-07-17) |
| 045 | Make standalone Reader a first-class practice mode and share completion orchestration with Daily | P2 | M | — | DONE (commit `cbfbf831`; 56 focused Reader/Daily tests + typecheck verified 2026-07-17) |
| 046 | Safely seed phoneme exercise types 10–14 | P1 | S | — | DONE (2026-07-18: production migration and ID↔slug/FK verification) |
| 047 | Propagate the real `speak_word` score into persistence | P1 | S | 046 | DONE (2026-07-18; see `TODO.md`) |
| 048 | Turn `topic_srs` rows into actionable Review sessions | P1 | M | 046 | DONE (2026-07-18; see `TODO.md`) |
| 049 | Add a secure cross-feature interest profile | P1 product | M | — | DONE (2026-07-18; see `TODO.md`) |
| 050 | Add the local/offline `error_correction` grammar exercise | P1 pedagogical | M | 046, 048 | DONE (2026-08-11 via Plan 075; 272 authored pairs across 128 decks, mixed topic-review producer) |
| 051 | Add the local/offline `conjugation_blank` grammar exercise | P1 pedagogical | M | 046, 048 | REJECTED / deferred (2026-08-11 via Plan 075; resume only with typed authored templates, accepted answers and fixtures) |
| 052 | Build Journal storage and offline-first sync | P1 product | L | RLS-INT/T56 baseline | DONE (2026-07-18; see `TODO.md`) |
| 053 | Correct Journal entries with Gemini and feed errors into SRS | P1 product | M | 049, 052 | DONE (2026-07-18; see `TODO.md`) |
| 054 | Ship the Journal UI and optional Daily entry point | P1 product | M | 052, 053 | DONE (2026-07-18; see `TODO.md`) |
| 055 | Add Reader tap-to-save into word bank | P2 product | M | T50 manual Reader QA | DONE (2026-07-18; commit `4873690c`) |
| 056 | Add `sentence_transformation` with remote generation/grading | P2 pedagogical | L | 046, 048 | DONE (2026-07-18; see `TODO.md`) |
| 057 | Add `translation_es_en` with hybrid grading | P2 pedagogical | L | 046, 048 | DONE (2026-07-18; see `TODO.md`) |
| 058 | Extract "My Words" into Tracking (saved words + phrases + lessons) | P2 product | L | — | DONE pending linked DB verify (2026-07-21: implementation committed — `tracked_items` migration+RLS, `/tracking` route, registry-by-`kind` render, `/words?tab=my-words`→`/tracking` redirect, on-demand "Repasar"; `lib/tracking`+`lib/sync`+`components/tracking` tests 81/81 green; all components ≤250 lines. Local Supabase is running; only `supabase db diff --linked` remains unverified) |
| 059 | Separate learning evidence and canonical lesson completion | P1 | L | — | DONE (2026-07-20: implementation complete; closed 2026-07-27 once Docker Desktop was available — `supabase db reset` applies all migrations cleanly including `lesson_completions`, `pnpm test:rls:integration` passes, 293/293 focused tests, typecheck clean) |
| 060 | Isolate local learning data by user | P1 security | L | 059 | DONE (2026-07-20: Dexie v21 scopes private local data, quarantines ambiguous legacy rows, and removes unsupported AI outbox targets) |
| 061 | Make SRS and outbox transactional and truthful | P1 | L | 059, 060 | DONE (steps 1-7 on `codex/061-transactional-srs-outbox`) |
| 062 | Fix exercise evidence attribution | P1 | L | 061 | DONE (2026-07-20 on `codex/062-evidence-attribution`) |
| 063 | Build the pronunciation production spine | P1 product/pedagogy | L | 059, 060, 062, 066 | DONE (2026-07-20 on `codex/063-pronunciation-spine`; steps 1-8 landed plus two follow-ups: Sound Lab now schedules a carrier-phrase production step alongside word production, and the coach's system prompt is now dynamically built server-side (fixing a pre-existing dead `buildSystemPrompt`) and caps feedback to one correction + retry phrase on a scored spoken chat turn; focused tests + typecheck pass; plan 066 dependency was still TODO when this executed) |
| 064 | Validate acoustic pronunciation assessment | P2 direction | L/XL | 063 | DONE — NO-SHIP (2026-07-21 steps 1-3: copy relabeled off "phoneme accuracy" to "word recognition accuracy"; provider-neutral `AcousticEvaluator` contract landed unwired. Step 4's real benchmark ran under plan 071: all 4 vowel contrasts failed the 0.85 agreement gate (0.309 overall) against speechocean762 — see `lib/pronunciation/acoustic/benchmark/decision.md`. ADR-064 updated 2026-07-27 to record the final NO-SHIP decision; no production score or vendor enabled) |
| 065 | Turn Tracking into verified review | P2 product/pedagogy | M | 058, 061, 062, 063, 066 | DONE (2026-08-10 via 073: exact phrase shadowing is activity-only without a target; explicit refs route to canonical owners; stale refs are skipped without guessing) |
| 066 | Create the canonical pronunciation target registry | P1 foundation | M | 062 | DONE (2026-07-21: `lib/pronunciation/targets/{types,registry,content-map,legacy-links}.ts` + ADR `docs/architecture/pronunciation-targets.md`; segmental/prosody/connected targets typed, evidence-capability validation rejects unshipped `acoustic` claims, content-map has zero dangling refs, legacy `contrast_id`/`focus=` adapters added without deleting legacy fields) |
| 067 | Build a pronunciation diagnostic and first-week prescription | P1 product/pedagogy | L | 060, 061, 063, 066 | DONE (2026-07-25: oral diagnostic route `/assessment/pronunciation` with capability preflight, seeded prompt selection, STT-only scoring, `not_measured` for unsupported dimensions, ≤3 priorities + 5-session prescription, guest/auth/offline persistence + RLS. Close-out: exclude `no_evaluator_available` from prescription (and filter legacy sessions in results UI); word-stress → perception-only selection; zero-severity scores not prioritized; finish-error client test fixed (stale mock assumed prosody+perception was invalid). Manual light/dark/custom-hue + unsupported-STT path checked. Focused assessment suites 168/168, persistence 8/8, typecheck + design-token lint clean. Acoustic vowels remain `not_measured` per 071 NO-SHIP; item variety landed in 072) |
| 068 | Build the pronunciation learning route | P1 product/pedagogy | L | 059, 063, 066, 067 | DONE (2026-08-10 via 073: five-stage route plus exact Route/Daily/Sound Lab mission launches; completion remains distinct from evidence. Content-authoring audit and sidebar capacity remain independent follow-ups) |
| 069 | Unify actionable pronunciation feedback | P1 pedagogy/product | L | 063, 066 | DONE (2026-07-26: single `PronunciationFeedbackModel` across lesson/Coach/Interview, `prioritizeFeedbackTarget` MIN_CONFIDENCE=0.6 + registry-membership gate, `NEXT_PUBLIC_PRONUNCIATION_ACTIONABLE_FEEDBACK_COPY` flag gates signal/focus copy without disabling controls — closed a gap where `CoachPanel.tsx` wasn't flag-gated like the other two surfaces. 292/292 focused tests, typecheck, `lint:design-tokens` clean) |
| 070 | Build goal-based oral missions | P1 product/pedagogy | L | 063, 066, 068, 069 | DONE (2026-08-10 via 073: deterministic mission engine plus active Route/Daily/Tracking/Sound Lab launch call sites, exact reconciliation and idempotent answer/session persistence; spoken evidence remains STT intelligibility only) |
| 071 | Benchmark a free Web-Audio vowel acoustic evaluator | P1 product/pedagogy | L | 064, 067 | DONE — NO-SHIP verdict (2026-07-25: full pipeline built and benchmarked against the real speechocean762 corpus at `D:\proyectos\speechocean762`, 14,374 items train+test. speechocean762 turned out to have no per-word timestamps (only 6/5000 utterances are single-word), so Task 6b added a proportional phoneme-count word-window heuristic (`word-window.ts`, NOT a forced aligner) to estimate word spans inside sentences. Verdict: all 4 v1 contrasts (iː/ɪ/æ/ʌ) fall short of the 0.85 ship threshold — iː closest at 0.619 agreement, ɪ worst at 0.116; see `lib/pronunciation/acoustic/benchmark/decision.md` for full numbers, confusion matrix, and the documented ambiguity between segmentation-heuristic noise vs. evaluator weakness as the likely cause. Per plan honesty rules, no-ship = no production change: vowel scoring in 067 stays `not_measured`, Task 11 (gate release) did not run. 255/256 focused tests pass (1 pre-existing unrelated failure, see 072 note), typecheck and lint clean) |
| 072 | Vary and expand the diagnostic item bank | P2 product/pedagogy | M | 067 | DONE (2026-07-25: per-session seed + 18-item word-stress bank sampled 5/run via seeded PRNG; scoring/evidence now run-size-relative (`perceptionItemCount`), not bank-size-relative; focused tests, typecheck and token lint pass. The stale `PronunciationAssessmentClient` finish-error fixture was fixed in the 067 close-out — mock now uses an actually-invalid prosody+STT score) |
| 074 | Sanitize Review and learning evidence before broader integration | P1 correctness/foundation | L | 059, 061, 062 | DONE (2026-08-10: pure topic-aware Review composition; canonical context-free answer→skills; completion removed from learning score; stable/idempotent Essential Words answers) |
| 073 | Connect all content to the shared learning loop | P1 product/pedagogy | XL | 074, 066 | DONE (2026-08-10: executable 3,230-entry manifest, shared theory identity, tracked phrases, canonical launches, deterministic Daily policy, audited evidence exits and separate activity/coverage/learning projections) |
| 075 | Consolidate active exercise capabilities and retire producer-less promises | P1 pedagogy/tech-debt | M | 073 | DONE (2026-08-11; audited capability inventory, authored error correction, deferred conjugation and shared safe primitives) |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- 019 replaces the stale 001 status in the current branch. Execute it before
  020, 021, or 022 so CI is a trustworthy verification gate again.
- Historical plan 001 may have been completed on another branch, but the current
  `.github/workflows/ci.yml` still contains non-blocking checks; treat 019 as
  the active CI plan.
- 003 has no code dependencies and should be executed immediately — key
  rotation is time-sensitive.
- 004 and 014 historically depended on 001 for a green test baseline. Plans
  009 and 013 later landed independently after replacement CI plan 019 was DONE.
- All other plans are independent and can be executed in any order after 001.
- 017 is independent of 016 but pairs well with it: better exercise generation improves daily plan and progress telemetry quality.
- 018 is independent of 016/017, but should be executed before adding more exercise types so new components target the shared feedback contract from day one.
- 020 completes and narrows the broad 016 progress-hub direction. If 016 is still
  active elsewhere, reconcile its remaining scope into 020 before implementation.
- 021 builds on the feedback contract from 018 but does not depend on 020.
- 022 should wait for 019 because route refactors need reliable tests.
- 023 is docs-only and can be done at any time.
- 024 should land before 025 so route-level bundle measurements use the smaller
  global-shell baseline.
- 025 owns the `/words` client-runtime split. 026 is independent because it
  changes server-side static content reads only.
- 027 supersedes 011. Passing the curriculum as a prop moved the import but did
  not prevent RSC serialization.
- 029 supersedes 012 with a current, vetted projection inventory.
- 026, 027, 028, and 029 can execute independently after the baseline is
  recorded.
- 030 is independent of the older infrastructure plans, but it should be
  executed before any UI that depends on `B1+` gating or a placement test so
  the test contract matches the curriculum instead of inventing its own scope.
- 033-036 are new backend hardening plans from the 2026-07-04 API audit. They
  can execute independently, but 033 should go first because it cleans up the
  durable-job lifecycle that later word-enrichment work depends on.
- 037-042 are performance follow-up plans from the 2026-07-05 Vercel React best
  practices audit. Execute 042 early so `pnpm build` is trustworthy again.
  037 should precede 038 because shell measurements are cleaner after
  request-level auth/client duplication is removed. 039 and 041 can run
  independently. 040 should wait for 038 so Server Component conversion is
  measured against the slimmer shell baseline.
- 044 should execute before unrelated product cleanup because it contains a
  production-data checkpoint and removes a dormant service-role Edge Function.
  It is independent of 045.
- 045 exposes the already-built standalone Reader and consolidates only its
  completion persistence. It does not depend on 044 and must not absorb the
  deferred `/api/words`, redirect, or `/test` decisions.
- The earlier 2026-07-17 reconciliation left 043 as the only atomic TODO.
  This product/pedagogy expansion adds 046–057. Roadmap 032 itself remains
  open only for the unchecked manual Reader QA (T50); its optional
  scale/operations phase is not an active implementation commitment.
- Plans 046–057 are a new product/pedagogy stream and do not reactivate the
  optional scale/operations phase of roadmap 032. Plan 043 remains separate and
  conditional.
- Execute the new stream in this order:
  1. Foundation bugs and personalization: 046 → 047, plus independent 049.
  2. Actionable grammar review: 048 → 050 and 051.
  3. Journal: close the isolated RLS baseline (TODO RLS-INT/T56), then
     052 → 053 → 054.
  4. Reader capture: finish T50 manual QA, then 055.
  5. Online grammar formats: 056 and 057 after 048; they may run independently.
- 046 owns the missing historical phoneme rows. Every later exercise-type plan
  still owns its own fail-fast ID↔slug migration so schema and code ship together.
- 048 must keep grammar-deck loading server-side because
  `lib/courses/grammar-deck/decks.ts` imports `fs`.
- 052 is blocked from execution—not planning—until RLS integration can run in
  a local/staging database. Never use production as the test environment.
- 055 deliberately waits for T50 so Reader's current completion/cache behavior
  has a verified baseline before tokenization changes its UI.
- 058 extracts the `my-words` tab into a dedicated `/tracking` route that also
  holds saved phrases and lessons. It is independent but pairs with 055 (word
  capture) and must confirm its data model first: words stay in `word_bank`; a
  new `tracked_items` table holds phrase/lesson favorites. It must keep the DB
  `diff --linked` clean and honor the offline-first + RLS rules recorded in
  `docs/database/schema-drift-and-state.md`.
- Execute the 2026-07-19 learning-loop stream in this order:
  1. 059 makes completion, quiz evidence and mastery truthful.
  2. 060 isolates every local learning/chat store by account.
  3. 061 makes offline SRS and sync acknowledgements transactional.
  4. 062 fixes target identity before new oral evidence is allowed to affect SRS.
  5. 063 connects theory, Sound Lab, phrase coach and oral chat.
  6. 064 is a gated acoustic-assessment spike; it must not block honest STT-based
     practice, but it must precede any Elsa-like accuracy promise.
  7. 065 turns saved/familiar items into exact practice queues. Its plan-058
     prerequisite is met: 058 is committed (see its row), so the earlier
     "uncommitted 058" block no longer applies.
- 059 precedes 060 because the user-scoped migration needs a canonical completion
  entity rather than migrating the existing synthetic answer shortcut.
- 061 precedes 062/063 so new Dictionary and spoken evidence cannot be lost or
  falsely reported as synchronized.
- 063 depends on 062 because Sound Lab must know which contrast each attempt
  evaluated before it can update mastery.
- Plan 066 is numbered after the original stream but executes **between 062 and
  063**. It supplies the target namespace that the production spine, diagnostic,
  route, feedback and missions must share; do not let 063 invent a local target
  model.
- Execute the extension stream after the integrity foundations in this order:
  1. 066 canonicalizes pronunciation targets after attribution plan 062.
  2. 063 builds/persists the controlled-production spine using those targets.
  3. 067 diagnoses targets and prescribes the first week.
  4. 068 turns that prescription into a visible learning route.
  5. 069 unifies correction/retry; it can run alongside 067/068 after 063+066.
  6. 070 uses the route and feedback contracts for contextual oral missions.
- Plan 064 remains an independent gate after 063. Its ship/partial/no-ship
  decision enriches 067/069 later, but neither may block honest STT-based
  practice or fabricate acoustic stress/rhythm/intonation scores.
- Plan 068 must not start while plan-059 course-progress edits remain
  uncommitted. Reconcile that working-tree overlap first.
- Execute the app-wide learning stream in the non-numeric dependency order
  **074 → 073 → 075**:
  1. 074 fixes topic-only Review, establishes one answer→skills authority,
     removes completion from learning scores and makes Essential Words attempts
     exact/idempotent.
  2. 073 consumes those invariants to connect theory identities, phrases,
     Route/Daily/Tracking/mission launches and separate progress projections.
  3. 075 uses 073's executable manifest to finish `error_correction`, mark
     `conjugation_blank` deferred if it still lacks templates and consolidate
     equivalent generator primitives.
  Do not implement 073 as one monolithic PR or start 075 from a stale/incomplete
  coverage manifest.

## Findings considered and rejected

- text_fragments RLS "null user_id bypass": policy logic actually *hides* NULL
  rows from users; works as designed for service-role cache writes.
- Gemini stream byte-cap "not enforced": the stream loop `return`s immediately
  after `safeClose()`; cap works.
- `.single()` crash on admin role check: supabase-js returns errors rather than
  throwing; check fails closed.
- JSON.parse uncaught in phrases route: parse is inside the model-fallback
  try/catch.
- `[SERVER-ACTIONS-13]` No Server Actions are currently in play: rejected.
  A repo scan on 2026-06-21 found no `use server` directives in tracked app
  source files, so the mutation surface remains API routes plus browser-side
  Supabase access.
- GeminiAdapter mic-track leak: stream lifecycle is owned by
  `useSharedMicStream`/`useVoiceRecorder`, which stop tracks.
- streak `yesterdayStr` UTC slice: only misfires at server TZ offsets beyond
  ±12h; servers run UTC. Hardening folded into the timezone finding (#4).
- admin-seed prompt injection via client `sounds`: admin-only, schema-validated,
  fails closed — marginal.
- Gemini key rotation/versioning governance: ops concern, not a code finding.
- `dayOfYear` off-by-one in daily-plan rotation: cosmetic variance only.
- `[STATE-10]` Zustand persistence risk: rejected. The repo currently has only
  `lib/stores/aiCoachStore.ts` and `lib/stores/uiSoundsStore.ts`, and neither
  uses Zustand `persist` middleware. There is no current evidence of private
  data being persisted via Zustand.
- Adding generic memoization to every Client Component: rejected because the
  measured cost is mounting and shipping inactive feature trees, not evidence
  of widespread expensive pure renders.
- Compressing the four existing PNG files: deferred because they are confined
  to auth presentation and are already served through `next/image`; route
  bundle and query work has higher leverage.
- “Words y Dictionary no están conectados”: rejected as a blanket finding.
  Objective Dictionary practice already reaches `answer_history`, activity
  sessions and word SRS; the real defects are target-id attribution and treating
  self-reported “known” as mastery (plans 062 and 065).
- Counting plain text chat turns as speaking progress: rejected. Only an actual
  spoken attempt with voice metadata may contribute speaking/pronunciation
  evidence (plan 063).
- Copying Elsa's single pronunciation score: rejected. The current signal is
  STT intelligibility/transcript matching; acoustic stress, rhythm and intonation
  require the gated validation in plan 064.
- Reusing the CEFR grammar placement as the pronunciation diagnostic: rejected.
  CEFR complexity informs material selection, but it does not identify segmental,
  prosodic or transfer targets; plan 067 persists a separate oral profile.
- Letting Gemini generate arbitrary missions and decide completion: rejected.
  Plan 070 keeps an authored mission contract and deterministic reducer; the
  model plays the role and emits validated events only.
- Creating a second scheduler for the pronunciation route: rejected. Plans
  066–070 must reuse canonical evidence, completion and SRS contracts from
  059–065.
