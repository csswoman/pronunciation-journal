{
  "tolerancePct": 10,
  "limitsKb": {
    "rootMainGzipKB": 168,
    "allChunksGzipKB": 2150
  },
  "notes": "Re-baselined 2026-09-05 at HEAD (dev). `allChunksGzipKB` still excludes the lazy CMUdict chunk (~939KB gzip, reported as `deferredChunksGzipKB`). History: 1275 = July 23 total (82 chunks); 1531 = Aug 18 (~111 chunks); 1780 = Aug 28 (136 chunks) after the WebGPU coach orb, scripted mission runner + catalog, syllable-level pronunciation scoring, journal notebook rebuild + UI sounds engine, SVGR illustration system, error-pattern taxonomy, and daily-plan grammar steps; 2046 = Sep 5 (154 chunks, ~2046KB gzip measured, reproducible on a clean build) after the hybrid offline-download feature (lib/offline, components/offline, LessonDownloadButton pulling in Dexie's full lib/db + IPA audio map), the courses/assessment/pronunciation-path redesign, and the phoneme-practice contrast-query split. Note: `allChunksGzipKB` sums every non-deferred chunk file written to .next/static/chunks — it is total shipped JS across all routes, not a single page's download; next/dynamic code-splitting a component does not lower this number, it only relocates bytes into a different chunk file (confirmed empirically 2026-09-05 — wrapping LessonDownloadButton in next/dynamic raised the count via added chunk-boundary overhead). SearchModal + the ~66KB content index are already lazy-loaded behind Sidebar's search-open state, per the Aug 28 note; that is not reflected in this metric. If this keeps climbing, either mark more genuinely-optional chunks as deferred probes (like CMUdict) or move the budget to a per-route first-load metric (e.g. next build's own First Load JS column) instead of aggregate build output. Biggest known eager chunks: Zod v4 (~63KB, many pages), the search index (~66KB gzip), lib/db's full Dexie schema (~1300 lines, pulled in by any client component importing @/lib/db, including LessonDownloadButton on every course-path row). CI allows +10% growth before failing."
}
